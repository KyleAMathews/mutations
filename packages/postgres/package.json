{
  "name": "@electric-sql/mutations-postgres",
  "version": "0.1.0",
  "description": "PostgreSQL integration for ElectricSQL mutations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest --run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "mutations",
    "postgresql",
    "electric-sql"
  ],
  "author": "Electric SQL",
  "license": "MIT",
  "dependencies": {
    "@electric-sql/mutations-core": "workspace:*",
    "@electric-sql/pglite": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
